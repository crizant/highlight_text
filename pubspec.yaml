name: highlight_text_plus
description: With this package you can highlight words and create specific styles and actions.
version: 2.0.0
homepage: https://github.com/crizant/highlight_text

environment:
  sdk: ">=2.14.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
